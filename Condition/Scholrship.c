#include<stdio.h>

int main() {
	int cast_Criteria, Grade_Criteria, Grade, income, Total_scholarship, scholarship;
	
	printf("Please Enter your Income:-");
	scanf("%d", &income);
	
	if (cast_Criteria) {
		if (open) {
			printf("No Scholarship");
		} else if (obc) {
			Total_scholarship = income + (scholarship=income * 0.25);
		} else if (sc) {
			Total_scholarship = income + (scholarship=income * 0.50);
		} else if (st) {
			Total_scholarship = income + (scholarship=income * 1);
		}
	} else {
		
	}
	
	return 0;
}
